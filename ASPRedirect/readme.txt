These files are needed if you already have an HTTP server
running on port 80. In such case, put wormageddonweb into
the root of your HTTP server files and run MyWormNET with
HTTP port set as 17080 (or anything else, as long as it's
also edited in Forwarder.asp as well).
